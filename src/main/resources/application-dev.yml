spring:
  datasource:
    url: jdbc:mysql://localhost:3306/serverloe?autoReconnect=true&useSSL=false
    username: sl_dev_user
    password: Adzarei66
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true

#
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: DB_CREATE.sql